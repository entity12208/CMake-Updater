# Uninstall existing CMake
winget uninstall CMake

# Install dependencies (if any are needed, install them manually)

# Install CMake via winget
winget install --id CMake.CMake

# Clean up (PowerShell doesn't have an exact "autoremove", but you can manually clean up if necessary)
# You can manually remove unwanted packages via the Control Panel or using winget
